connections {
  snowflake {
    url = ${SNOWFLAKE_URL}
    properties {
      role = access_tool
      warehouse = provisioning
      user = access_tool_user
      private_key_file = ${SNOWFLAKE_PRIVATE_KEY_FILE}
    }
  }
}
