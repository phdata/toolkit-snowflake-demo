  connections {
    snowflake = {
      url = ${SNOWFLAKE_URL}
      properties {
        role = ACCESS_DEMO_ROLE
        warehouse = ACCESS_DEMO_WH # remove this once service accounts set up
        user = ADMIN # change to access_demo_user
        password = ${SNOWFLAKE_PASSWORD}
      }
    }
  }

  access {
    connection = ${connections.snowflake}
}